# Copy to terraform.tfvars for local runs. CI reads config/terraform-environments.yaml (see scripts/ci/terraform-from-config.rb).
# Values below match environments.dev in that file.
project_id    = "anomaly-detection-dev-496103"
region        = "australia-southeast1"
environment   = "dev"
